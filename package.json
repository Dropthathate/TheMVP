{
  "name": "somasync-ai-mvp",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild server/_core/index.ts --platform=node --bundle --format=esm --outdir=dist"
  },
  "dependencies": {
    "esbuild": "^0.20.0"
  }
}